apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: shikanime-studio
spec:
  ports:
    - name: http
      port: 5000
      targetPort: http
  selector:
    app.kubernetes.io/name: mlflow
