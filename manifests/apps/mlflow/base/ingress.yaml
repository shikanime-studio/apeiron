apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mlflow
  namespace: shikanime-studio
spec:
  defaultBackend:
    service:
      name: mlflow
      port:
        name: http
  tls:
    - hosts:
        - mlflow
