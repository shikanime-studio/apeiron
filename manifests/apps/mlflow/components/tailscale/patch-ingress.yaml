apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mlflow
  annotations:
    tailscale.com/tags: tag:web
  labels:
    tailscale.com/proxy-class: mlflow
spec:
  ingressClassName: tailscale
